# Training extras for SFT + GRPO on Qwen3.5-9B (H200).
# Install on top of requirements.txt:  pip install -r requirements-train.txt
#
# Pins reflect first-class Qwen3.5 (Gated DeltaNet + Gated Attention) support.

torch>=2.4,<2.7
transformers>=4.50          # Qwen3.5 modeling code
trl>=0.12,<0.14             # GRPOTrainer
accelerate>=1.1
peft>=0.13                  # LoRA for GRPO
datasets>=3.0
omegaconf>=2.3              # YAML config composition
wandb>=0.18

# Inference (judges + frozen student) — install separately on H200 hosts:
#   vllm>=0.7
#   flash-attn>=2.6
#   bitsandbytes>=0.44   # AWQ judge loading
#
# Kept commented because they require GPU wheels and break local CPU dev installs.
