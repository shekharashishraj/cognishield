# Secrets & local environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.so
.Python
.venv/
.venv-*/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Training / model outputs (root `out/` — training/.gitignore only covers training/out)
/out/

# Local virtual env for training stack (large; never commit)
/.venv-train/

# OS / editors
.DS_Store
.idea/
.vscode/

# Large generated data files (should never be committed to GitHub)
data/generated/**/errors.jsonl
data/generated/**/events.jsonl
data/generated/**/run.log
